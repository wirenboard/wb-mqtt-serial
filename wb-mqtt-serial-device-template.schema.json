{
  "type": "object",
  "properties": {
    "device_type": {
      "type": "string",
      "minLength": 1
    },
    "device": {
      // must be "$ref": "wb-mqtt-serial.schema.json#/definitions/customDevice" but valijson doesn't support loading external refs
      // so we append definitions in c++ code
      "$ref": "#/definitions/customDevice"
    }
  },
  "required": ["device_type", "device"]
}
